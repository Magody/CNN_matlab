addpath(genpath('cnn_utils'));
X = rand(3, 3, 2);

% a = zeroPad(X, 2);

% TEST CONV FORWARD
m = 10; n_H = 5; n_W = 7; n_C = 8;

A_prev = rand(n_H, n_W, n_C, m);
W = np.random.randn(3,3,4,8);
b = np.random.randn(1,1,1,8);
hparameters.pad = 1;
hparameters.stride = 2= {"pad" : 1,
               "stride": 2}

Z, cache_conv = conv_forward(A_prev, W, b, hparameters)
    
